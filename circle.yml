machine:
    php:
        version: 7.0.4

dependencies:
    pre:
      - sudo apt-get update
      - sudo apt-get install php7-imagick

test:
    post:
        - zip -r $CIRCLE_ARTIFACTS/build.zip .

general:
    artifacts:
        - "$CIRCLE_ARTIFACTS/build.zip"